{% extends "layout.html" %}
{% block title %}{{ this.title }}{% endblock %}
{% block body %}
        {% if this.title %}
        <div class="row">
            <h1>{{ this.title }}</h1>
        </div>
        {% endif %}
        <h2>{{ this.subtitle }}</h2>
        <p>{{ this.abstract }}</p>
        <p>{{ this.description }}</p>
        {% for author in site.query('/authors').all() %}
            {% if author._id == this.author %}
                <a href="{{ author|url}}">{{ author.name }}</a>
            {% endif %}
        {% endfor %}
{% endblock %}
